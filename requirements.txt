# PronoFoot - Runtime dependencies
#
# Le cœur du projet n'utilise QUE la bibliothèque standard Python (json, sqlite3,
# math, urllib...). Aucune dépendance tierce n'est requise pour faire tourner le
# pipeline, l'ingestion et le moteur de pronostic.
#
# Les dépendances externes (requests, pandas...) utilisées par quelques scripts
# d'ingestion optionnels sont listées au cas par cas dans ces scripts.
